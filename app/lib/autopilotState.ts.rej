@@ -7,6 +7,11 @@
   state: string;
 };
 
+export type AutopilotStartLock = {
+  ts: number;
+  reason: string;
+};
+
 const KEY_PRIMARY = "autopilot:primary";
 const KEY_ENABLED = "autopilot:enabled";
 const KEY_INTERVAL = "autopilot:interval_seconds";
@@ -63,9 +68,13 @@
   return heartbeat;
 }
 
-export async function acquireAutopilotStartLock(exSeconds = 120): Promise<boolean> {
+export async function acquireAutopilotStartLock(reason: string, exSeconds = 120): Promise<boolean> {
   const store = getStore();
-  return await store.set(KEY_START_LOCK, String(Date.now()), {
+  const lock: AutopilotStartLock = {
+    ts: Date.now(),
+    reason,
+  };
+  return await store.set(KEY_START_LOCK, lock, {
     exSeconds,
     nx: true,
   });
